@model Room
   <div>
       <h1>DETAILS</h1>
    </div>
<div class="whiteColorDiv">
    <h3 class="pull-right ">@Model.Price.ToString("c")</h3>
    <h3>@Model.Name</h3>
    
    <p>@Model.ShortDescription</p>
    @foreach (var item in Model.Reservations)
    {
        <h4>@item.ReservationFrom</h4>
    }
    
</div>